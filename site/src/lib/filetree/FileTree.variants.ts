import type { Variants } from 'kitbook'
import type Component from './FileTree.svelte'

export const variants: Variants<Component> = [
  {
    name: 'Desktop',
    description: 'Describe this variant',
    props: {
    },
  },
]
